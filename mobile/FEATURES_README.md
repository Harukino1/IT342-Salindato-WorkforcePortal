Mobile

This repository's mobile module hasn't been fully reorganized yet. Recommended approach:

- Create `features` packages under `app/src/main/java/.../features/` for `auth`, `attendance`, and `leave`.
- Move platform-specific screens and view models into their corresponding feature folders.

For now this file documents the intended vertical-slice layout for the mobile app.
